# Base URL for the API (contact form + chatbot).
# Leave empty to call same-origin "/api/..." routes (used on Vercel).
# Set to something like http://localhost:5000 only if you run server.js separately
# without the CRA dev proxy.
REACT_APP_API_URL=
